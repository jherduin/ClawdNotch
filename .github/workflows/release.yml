name: Release

on:
  push:
    tags:
      - "v*"

# Nécessaire pour que `gh release create` puisse publier la Release et l'asset.
permissions:
  contents: write

jobs:
  release:
    name: Build, empaquetage .dmg et GitHub Release
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Déterminer la version depuis le tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Sélectionner la dernière Xcode disponible
        # XcodeGen génère le projet au format Xcode 16 (objectVersion 77) ;
        # le 15.4 par défaut du runner ne sait pas le lire.
        run: |
          XC=$(ls -d /Applications/Xcode_*.app | sort -V | tail -1)
          echo "Xcode sélectionné : $XC"
          sudo xcode-select -s "$XC"
          xcodebuild -version

      - name: Installer XcodeGen
        run: brew install xcodegen

      - name: Générer le projet Xcode
        run: xcodegen generate

      - name: Compiler (Release, sans signing)
        run: |
          xcodebuild \
            -project ClawdNotch.xcodeproj \
            -scheme ClawdNotch \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Créer le .dmg
        run: |
          APP="build/Build/Products/Release/ClawdNotch.app"
          DMG="ClawdNotch-${{ steps.version.outputs.version }}.dmg"
          STAGING="$(mktemp -d)"
          cp -R "$APP" "$STAGING/"
          ln -s /Applications "$STAGING/Applications"
          hdiutil create \
            -volname "ClawdNotch" \
            -srcfolder "$STAGING" \
            -ov \
            -format UDZO \
            "$DMG"
          echo "DMG=$DMG" >> "$GITHUB_ENV"

      - name: Créer la GitHub Release et attacher le .dmg
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            "$DMG" \
            --title "$GITHUB_REF_NAME" \
            --generate-notes
